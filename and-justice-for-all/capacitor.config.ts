import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'And Justice For All',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
